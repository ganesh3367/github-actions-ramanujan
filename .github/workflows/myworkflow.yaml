name: "print hello world"
on:
  push:

jobs:
  hello:
    runs-on: macos-latest
    steps:
      - name: Print hello world
        run: echo "Hello world"

  second-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print hello world
        run: echo "Hello world"